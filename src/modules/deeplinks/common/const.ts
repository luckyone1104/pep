import { BASE_API_URL } from '../../../api';

export const DEEPLINKS_URL = `${BASE_API_URL}/deeplinks`;
export const DEEPLINKS_STATES_URL = DEEPLINKS_URL.concat('/states');
export const DEEPLINKS_USERS_URL = DEEPLINKS_URL.concat('/users');
